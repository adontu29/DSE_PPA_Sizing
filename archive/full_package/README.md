# Archived Full Package

This folder contains the earlier modular Bellona sizing package. It is kept for
traceability and comparison. The simplified branch uses `simple_sizing.py` at
the repository root as the readable report code path.
